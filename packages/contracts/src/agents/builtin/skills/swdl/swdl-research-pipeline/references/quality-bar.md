# Quality bar
Hypotheses need `status` + `summary`. Prefer board columns over orphan docs.
A hypothesis is `validated` only after the validate stage (interview/experiment evidence linked via `informs`); insufficient evidence sends it back to sources, not straight to planning.
Monitoring snapshots (`metric_snapshot`) link to their `kpi` via `snapshotted_from`.
