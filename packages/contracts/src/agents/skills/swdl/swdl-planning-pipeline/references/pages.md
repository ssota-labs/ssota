# Pages (planning)

- `manager/initiatives`, `manager/approvals` — initiative list + human approval surface
- `tpl/initiative/planning/*` — prd (DocumentEditor + approval), features (with create/addAction), stories (with estimate column; DoR itself is Delivery-owned)
- `tpl/initiative/launch/plan` — launch_plan authoring + Approval tab (Cycle E `s-plan`)
- `tpl/initiative/launch/docs` — release_note / runbook authoring (create actions)
- `tpl/initiative/retrospective/review` — retro authoring (planning writes the retro; research supplies metrics)
- `tpl/initiative/retrospective/metrics` — metric snapshots + trend (read as retro input)
