# Pages (qa)

- `tpl/initiative/qa/test-plan` — test plan surface: TestResults tab (pass/fail/blocked outcomes) + `verifies` RelationEditor (test_plan → pull_request | user_story)
- `development/pull-requests` — PR review inbox (only `approved` PRs enter QA)
