# Pages (research)

- `research/hypotheses` — hypothesis board; supports create, plus an evidence RelationEditor for linking `informs` edges from evidence nodes
- `research/user` — user research surface (rebuilt): interview DataTable with create affordance for `user_research` rows
- `research/market` — market research surface with create affordances (`market_research`, competitors via `references`)
- `tpl/initiative/research/*` — initiative-scoped research pages
- `tpl/initiative/retrospective/metrics` — where post-launch `metric_snapshot` rows land (Cycle E `s-monitor` monitoring output; supports snapshot create + trend chart)
