import { PitchDeck } from "@/components/pitch-deck/pitch-deck";

export const metadata = {
  title: "Pax Humana / SSOTA Pitch Deck",
};

export default function PitchDeckPage() {
  return <PitchDeck />;
}
